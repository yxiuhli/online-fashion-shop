'use client';

const UpdateButton = () => {
  return (
    <button className="bg-red-500 dark:hover:bg-red-600 text-white p-2 rounded-md cursor-pointer disabled:bg-pink-200 disabled:cursor-not-allowed">
      Update
    </button>
  );
};

export default UpdateButton;
